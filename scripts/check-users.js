const { createClient } = require("@supabase/supabase-js"); require("dotenv").config({ path: ".env.local" }); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); async function test() { console.log("🔍 Vérification des utilisateurs..."); const { data: users, error } = await supabase.from("users").select("email, first_name, last_name").limit(5); if (error) { console.error("❌ Erreur:", error); } else { console.log("✅ Utilisateurs trouvés:", users.length); users.forEach(user => { console.log(`- ${user.email} (${user.first_name} ${user.last_name})`); }); } } test();
