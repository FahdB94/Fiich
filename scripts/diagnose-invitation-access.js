const { createClient } = require("@supabase/supabase-js"); require("dotenv").config({ path: ".env.local" }); const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); async function test() { console.log("🔍 Test rapide des invitations..."); const { data: invitations, error } = await supabase.from("invitations").select("*").limit(3); if (error) { console.error("❌ Erreur:", error); } else { console.log("✅ Invitations trouvées:", invitations.length); invitations.forEach(inv => { console.log(`- ${inv.invited_email} (${inv.invitation_token})`); }); } } test();
